# 🫧 BadBleep Box — private cheat registry

> Internal doc. Deliberately a dot-file, not linked from README or the app.
> The shipped bundle only contains **hashes** of these codes
> (`src/engine/bleepbox.js`), so reading the deployed JS won't reveal them.

## Where

**You tab → ⚙ Settings → scroll to the bottom → "🫧 BadBleep Box"** — type a
code and hit *bleep* (or Enter). Codes are case/space/punctuation-insensitive.

## Live codes

`ILoveYouMerci` is deliberately **not** in the in-app tap-list (the grid of
chips under the BadBleep input) — every other code is discoverable that way,
by design. This one you have to know and type in full. That's the point.

| Code | Hash (`36`) | Effect |
|---|---|---|
| `ILoveYouMerci` | `14gauzg` | 💖 **Secret.** +100,000 Loks (also counted in earned-all-time). Not listed as a tappable chip — type it. |
| `SupaBleepMode` | `10j8w3a` | 🫧 Dev mode: unlocks **everything** for testing — LokPass, all 24 themes, all effects & skies, all 38 studio modules, every cosmetic (name colors, frames, reaction packs, accents, blot borders, papers, LilLok gear), Big Battle, Sound Lab, +9,999 Loks |
| `cincoorso` | `piyk6r` | Activates the comeback tease line |
| `mercmerc` | `1qod2qa` | 🪙 +200 Loks |
| `inkoverflow` | `9axo14` | 🖋️ Max ink & bond, revives LilLok |
| `phoenix` | `1p6f89b` | 🔥 Revive from stasis (+50 ink, +10 bond) |
| `FodSkip` | `1nm6zsq` | 📖 Forces a fresh Flip of the Day |
| `pinball` | `1pmuvis` | 🔐 Sets a random 4-digit session PIN |
| `whisper` | `lrxbia` | 🗣️ Unlocks all voice packs |
| `nightmode` | `mlke63` | 🌙 Forces a dark theme |
| `doubledown` | `cvxw4z` | ✨ Doubles current Loks |
| `resolve` | `i5celo` | 📋 Completes all active quests, pays their rewards |
| `vibemode` | `i3e195` | 🎉 Cycles the level-up celebration style |
| `CanteDev616` | `y468tj` | 🔧 Unlocks Dev Mode / debugging options |
| `Tokens10K` | `2cr2oi` | 🪙 +10,000 Loks |
| `ChestRain` | `ebi4gl` | 🎁 +10 random chests, for testing |
| `ChestSpree` | `hqr2d0` | 🎁 +18 chests (3 per rarity), for testing |
| `ChestView` | `ebku92` | Toggles the chest viewer |
| `ChestOne` | `pnznb9` | 🎁 +1 chest |
| `ChestFive` | `ebamur` | 🎁 +5 chests |

The chest codes' original hashes (`10vspvk`, `p3wyw4`, `r6q49f`, `1cii4i8`,
`1cii4ic`) had no plaintext recorded anywhere — not in this doc, not in the
app — so they were unreachable. Replaced with the fresh hashes above; the
old ones are dead and safe to ignore if you see them in old commits.

## Adding a new code

1. Compute the hash:
   ```bash
   node -e "const n=s=>s.toLowerCase().replace(/[^a-z0-9]/g,'');const h=s=>{let x=0;for(const c of n(s))x=(x*31+c.charCodeAt(0))>>>0;return x.toString(36)};console.log(h('YourNewCode'))"
   ```
2. Add `"<hash>": { fx: "<effect>" }` to `CODES` in `src/engine/bleepbox.js`.
3. Implement the `fx` branch in `onCheat` in `src/App.jsx`.
4. Record it in the table above. Do not write the plaintext anywhere else.

## Notes

- Effects are **local-device only** (they mutate the local save blob); nothing
  is granted server-side, so codes can't inflate the party feed or other users.
- `SupaBleepMode` is additive — it won't remove anything you already own, and
  the state persists via the normal save. To reset: Settings → Clear all local data.
- The 31-rolling hash is anti-casual, not cryptographic. Fine for fun codes;
  don't gate anything valuable behind it.
